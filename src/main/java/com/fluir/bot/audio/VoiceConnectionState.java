package com.fluir.bot.audio;

/**
 * Sunucu ses bağlantı durumlarını temsil eden enum.
 */
public enum VoiceConnectionState {
    DISCONNECTED,
    CONNECTING,
    CONNECTED,
    DISCONNECTING
}
